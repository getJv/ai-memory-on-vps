# Local secrets

Keep real secret files in this directory with mode `0600`. They are ignored by Git. Generate the authentication token and token pepper with `../scripts/generate-secrets.sh`, then create `openrouter-api-key` manually.
